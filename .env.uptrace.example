# ==============================================================================
# Uptrace Configuration
# ==============================================================================
# Copy this file to .env.uptrace and fill in the values
# These values are used by deploy/compose/compose.uptrace.yaml

# ----------------------------------------------------------------------------
# ClickHouse Database (Backend Storage)
# ----------------------------------------------------------------------------
# Password for ClickHouse uptrace user
# Generate with: openssl rand -base64 32
CLICKHOUSE_PASSWORD=changeme_clickhouse_password

# ----------------------------------------------------------------------------
# PostgreSQL Database (Metadata Storage)
# ----------------------------------------------------------------------------
# Password for PostgreSQL uptrace user
# Generate with: openssl rand -base64 32
UPTRACE_POSTGRES_PASSWORD=changeme_postgres_password

# ----------------------------------------------------------------------------
# Uptrace Project Configuration
# ----------------------------------------------------------------------------
# Project ID (default: 1 for first project)
UPTRACE_PROJECT_ID=1

# Project token (used for authentication)
# Generate with: uuidgen | tr '[:upper:]' '[:lower:]'
# Or: openssl rand -hex 16
UPTRACE_PROJECT_TOKEN=changeme_project_token

# ----------------------------------------------------------------------------
# Uptrace Admin Credentials
# ----------------------------------------------------------------------------
# Admin email (for initial login)
UPTRACE_ADMIN_EMAIL=admin@pressograph.local

# Admin password (for initial login)
# Use a strong password in production
UPTRACE_ADMIN_PASSWORD=changeme_admin_password

# ----------------------------------------------------------------------------
# Uptrace Security
# ----------------------------------------------------------------------------
# Secret key for encryption (used for sessions, cookies, etc.)
# Generate with: openssl rand -hex 32
UPTRACE_SECRET_KEY=changeme_secret_key_must_be_at_least_32_characters_long

# ----------------------------------------------------------------------------
# Uptrace Site Configuration
# ----------------------------------------------------------------------------
# Public URL for Uptrace (used in emails, links, etc.)
UPTRACE_SITE_ADDR=https://dev-uptrace.infra4.dev

# ----------------------------------------------------------------------------
# Optional: SMTP Configuration for Email Alerts
# ----------------------------------------------------------------------------
# Uncomment and configure if you want email alerts
# UPTRACE_SMTP_ENABLED=true
# UPTRACE_SMTP_HOST=smtp.gmail.com
# UPTRACE_SMTP_PORT=587
# UPTRACE_SMTP_USERNAME=your-email@gmail.com
# UPTRACE_SMTP_PASSWORD=your-app-password
# UPTRACE_SMTP_FROM=uptrace@pressograph.local
